# Copy to .env and adjust. .env is gitignored; this file is committed so
# anyone cloning the repo can see what configuration exists.
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=labpassword
APP_NAME=fastapi-redis-lab