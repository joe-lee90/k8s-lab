apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: lab
spec:
  type: ClusterIP        # cluster-internal only. Redis must never be exposed.
  selector:
    app: redis           # claims every pod carrying this label
  ports:
    - name: redis
      port: 6379
      targetPort: 6379